openapi: 3.1.0
info:
  title: Kion Cloud Operations Accounts GCP IAM Roles API
  description: The Kion Public API provides programmatic access to manage cloud operations, governance, compliance, and financial management across AWS, Azure, GCP, and OCI. Kion is a self-hosted cloud operations platform that consolidates account provisioning, access management, compliance enforcement, and FinOps into a single interface. The API uses Bearer token authentication via Kion App API Keys and is accessible at the /api/v3/ path of your Kion instance.
  version: 3.10.0
  contact:
    name: Kion Support
    url: https://support.kion.io
  license:
    name: Proprietary
    url: https://kion.io/why-kion/pricing-and-licensing/
servers:
- url: https://{kion-instance}/api/v3
  description: Kion Instance API Server
  variables:
    kion-instance:
      default: your-kion-instance.example.com
      description: Your Kion deployment hostname
security:
- bearerAuth: []
tags:
- name: GCP IAM Roles
  description: Manage GCP IAM roles
paths:
  /gcp-iam-role:
    get:
      operationId: listGcpIamRoles
      summary: Kion List GCP IAM roles
      description: Returns a list of all GCP IAM roles.
      tags:
      - GCP IAM Roles
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/GcpIamRole'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: createGcpIamRole
      summary: Kion Create a GCP IAM role
      tags:
      - GCP IAM Roles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GcpIamRoleCreate'
      responses:
        '201':
          description: Role created
        '401':
          $ref: '#/components/responses/Unauthorized'
  /gcp-iam-role/{id}:
    get:
      operationId: getGcpIamRole
      summary: Kion Get a GCP IAM role
      tags:
      - GCP IAM Roles
      parameters:
      - $ref: '#/components/parameters/IdParam'
      responses:
        '200':
          description: Successful response
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateGcpIamRole
      summary: Kion Update a GCP IAM role
      tags:
      - GCP IAM Roles
      parameters:
      - $ref: '#/components/parameters/IdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GcpIamRoleUpdate'
      responses:
        '200':
          description: Role updated
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteGcpIamRole
      summary: Kion Delete a GCP IAM role
      tags:
      - GCP IAM Roles
      parameters:
      - $ref: '#/components/parameters/IdParam'
      responses:
        '204':
          description: Role deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  schemas:
    GcpIamRoleCreate:
      type: object
      required:
      - name
      - gcp_id
      - role_permissions
      properties:
        name:
          type: string
        description:
          type: string
        gcp_id:
          type: string
        gcp_role_launch_stage:
          type: integer
        role_permissions:
          type: array
          items:
            type: string
        labels:
          type: object
          additionalProperties:
            type: string
    GcpIamRole:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        gcp_id:
          type: string
        gcp_role_launch_stage:
          type: integer
        role_permissions:
          type: array
          items:
            type: string
        owner_users:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
        owner_user_groups:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
        labels:
          type: object
          additionalProperties:
            type: string
        created_at:
          type: string
          format: date-time
    GcpIamRoleUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        gcp_role_launch_stage:
          type: integer
        role_permissions:
          type: array
          items:
            type: string
        labels:
          type: object
          additionalProperties:
            type: string
  responses:
    Unauthorized:
      description: Authentication failed or API key is missing/invalid
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 401
              message:
                type: string
                example: Unauthorized
    NotFound:
      description: The requested resource was not found
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 404
              message:
                type: string
                example: Not found
  parameters:
    IdParam:
      name: id
      in: path
      required: true
      description: The unique identifier of the resource in Kion
      schema:
        type: integer
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 'Kion App API Key. Generate in Kion under your user profile settings. Use in the Authorization header as: Bearer YOUR_API_KEY'
