naftiko: 1.0.0-alpha2
info:
  label: Kion Cloud Operations API — Azure Roles
  description: 'Kion Cloud Operations API — Azure Roles. 5 operations. Lead operation: Kion List Azure roles. Self-contained
    Naftiko capability covering one Kion business surface.'
  tags:
  - Kion
  - Azure Roles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KION_API_KEY: KION_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloud-operations-azure-roles
    baseUri: https://{kion-instance}/api/v3
    description: Kion Cloud Operations API — Azure Roles business capability. Self-contained, no shared references.
    resources:
    - name: azure-role
      path: /azure-role
      operations:
      - name: listazureroles
        method: GET
        description: Kion List Azure roles
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createazurerole
        method: POST
        description: Kion Create an Azure role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: azure-role-id
      path: /azure-role/{id}
      operations:
      - name: getazurerole
        method: GET
        description: Kion Get an Azure role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateazurerole
        method: PATCH
        description: Kion Update an Azure role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteazurerole
        method: DELETE
        description: Kion Delete an Azure role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.KION_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloud-operations-azure-roles-rest
    port: 8080
    description: REST adapter for Kion Cloud Operations API — Azure Roles. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/azure-role
      name: azure-role
      description: REST surface for azure-role.
      operations:
      - method: GET
        name: listazureroles
        description: Kion List Azure roles
        call: cloud-operations-azure-roles.listazureroles
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createazurerole
        description: Kion Create an Azure role
        call: cloud-operations-azure-roles.createazurerole
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/azure-role/{id}
      name: azure-role-id
      description: REST surface for azure-role-id.
      operations:
      - method: GET
        name: getazurerole
        description: Kion Get an Azure role
        call: cloud-operations-azure-roles.getazurerole
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updateazurerole
        description: Kion Update an Azure role
        call: cloud-operations-azure-roles.updateazurerole
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteazurerole
        description: Kion Delete an Azure role
        call: cloud-operations-azure-roles.deleteazurerole
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloud-operations-azure-roles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Kion Cloud Operations API — Azure Roles. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: kion-list-azure-roles
      description: Kion List Azure roles
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloud-operations-azure-roles.listazureroles
      outputParameters:
      - type: object
        mapping: $.
    - name: kion-create-azure-role
      description: Kion Create an Azure role
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: cloud-operations-azure-roles.createazurerole
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: kion-get-azure-role
      description: Kion Get an Azure role
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloud-operations-azure-roles.getazurerole
      outputParameters:
      - type: object
        mapping: $.
    - name: kion-update-azure-role
      description: Kion Update an Azure role
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: cloud-operations-azure-roles.updateazurerole
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: kion-delete-azure-role
      description: Kion Delete an Azure role
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: cloud-operations-azure-roles.deleteazurerole
      outputParameters:
      - type: object
        mapping: $.
